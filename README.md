A command line interpreter for Linux environment implemented in C. 

The shell creates child processes for concurrent excecution, can take multiple commands, and has piping and redirection capabilities.
